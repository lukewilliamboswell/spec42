[package]
name = "sysml_tokens"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "SysML v2 semantic tokenization for editor highlighting (WASM and LSP neutral)"

[dependencies]
serde = { workspace = true, optional = true }
sysml_query = { path = "../sysml_query" }

[features]
default = []
serde = ["dep:serde"]
